boolean isCircular(Node head)
    {
	   if(head.next==null)
	   {
	       return false;
	   }
	   
	   Node curr=head;
	   while(head.next!=null)
	   {
	       if(head.next==curr)
	       return true;
	       
	       head=head.next;
	   }
	   return false;
    }

